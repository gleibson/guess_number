#include<time.h>
#include<time.h>
#include<cstdlib>
#include<string>

#pragma once

class Game  //class called game
{

private:
    int guess, number;  // two variables on private scope
    std::string again;

public:
    Game();
};
